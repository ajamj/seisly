---
phase: v1.0-production-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "pyproject.toml",
  "python/seisly/__init__.py",
  "crates/seisly_plugin/src/python.rs",
  "docs/user-manual/book.toml",
  "CHANGELOG.md"
]
autonomous: true
requirements: [R8.2]
must_haves:
  truths:
    - "No occurrences of 'seisly' in user-facing Python package or docs"
    - "mdBook can be built and serves content from docs/user-manual"
    - "Critical TODO markers in core crates are addressed or converted to issues"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package name 'seisly'"
    - path: "docs/user-manual/book.toml"
      provides: "mdBook configuration"
  key_links:
    - from: "python/seisly/__init__.py"
      to: "PyO3 extension"
      via: "import"
---

<objective>
Standardize the 'Seisly' brand across all layers and initialize professional user documentation.

Purpose: Consistency and brand identity for production release.
Output: Renamed Python package, initialized mdBook, and cleaned-up codebase.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.0-production-release/v1.0-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global Branding Rename</name>
  <files>pyproject.toml, python/seisly/__init__.py, crates/seisly_plugin/src/python.rs, benches/Cargo.toml, docs/development/plugin-development.md</files>
  <action>
    - Rename Python package from 'seisly' to 'seisly' in pyproject.toml and imports.
    - Update PyO3 module name in crates/seisly_plugin/src/python.rs (pub fn seisly).
    - Update benches name in benches/Cargo.toml.
    - Search and replace 'seisly' with 'Seisly' in all markdown documentation.
  </action>
  <verify>
    <automated>grep -ri "seisly" . | grep -v ".git" | grep -v "CHANGELOG" | grep -v "PLAN.md"</automated>
  </verify>
  <done>Zero occurrences of 'seisly' in active code and documentation.</done>
</task>

<task type="auto">
  <name>Task 2: mdBook Initialization</name>
  <files>docs/user-manual/book.toml, docs/user-manual/src/SUMMARY.md, docs/user-manual/src/index.md</files>
  <action>
    - Initialize mdBook in docs/user-manual.
    - Configure book.toml with title "Seisly User Manual".
    - Create basic structure in SUMMARY.md (Introduction, Installation, Getting Started, Troubleshooting).
    - Migrate content from top-level README.md and index.md into the book.
  </action>
  <verify>
    <automated>mdbook build docs/user-manual</automated>
  </verify>
  <done>Documentation builds successfully and reflects the new Seisly branding.</done>
</task>

<task type="auto">
  <name>Task 3: Quality Audit & TODO Cleanup</name>
  <files>crates/seisly_core/src/lib.rs, crates/seisly_io/src/lib.rs, crates/seisly_app/src/main.rs</files>
  <action>
    - Scan for 'TODO' and 'todo!' macros in core, io, and app crates.
    - Clear critical TODOs (e.g., missing error handling, placeholders in I/O).
    - Convert non-critical TODOs into GitHub issues or track in CONCERNS.md.
  </action>
  <verify>
    <automated>grep -rnE "TODO|todo!" crates/seisly_core crates/seisly_io crates/seisly_app | wc -l</automated>
  </verify>
  <done>Critical codebase markers resolved; remaining items documented.</done>
</task>

</tasks>

<verification>
Check for brand consistency in Python REPL (import seisly) and mdBook build output.
</verification>

<success_criteria>
- Python package installable as 'seisly'.
- User manual available as an mdBook.
- Codebase feels clean and ready for v1.0.
</success_criteria>

<output>
After completion, create .planning/phases/v1.0-production-release/v1.0-01-SUMMARY.md
</output>
