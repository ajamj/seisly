---
phase: v1.0-production-release
plan: 02
type: execute
wave: 2
depends_on: ["v1.0-01"]
files_modified: [
  "crates/seisly_io/Cargo.toml",
  "crates/seisly_io/src/cache.rs",
  "crates/seisly_app/Cargo.toml",
  "crates/seisly_app/src/main.rs",
  "crates/seisly_render/src/surface.rs"
]
autonomous: true
requirements: [R8.1, ERR-01]
must_haves:
  truths:
    - "Repeated access to the same seismic bricks is faster after moka integration"
    - "Application shows a native error dialog on panic instead of a silent crash"
    - "Surface normals are computed correctly, improving 3D lighting"
  artifacts:
    - path: "crates/seisly_io/src/cache.rs"
      provides: "moka-based LRU brick cache"
    - path: "crates/seisly_app/src/main.rs"
      provides: "Panic hook and Sentry initialization"
  key_links:
    - from: "seisly_io::SeismicReader"
      to: "moka::sync::Cache"
      via: "get_brick"
---

<objective>
Implement out-of-core caching for performance and professional error handling for stability.

Purpose: Production-grade performance and reliability.
Output: Fast seismic data access and robust crash reporting.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.0-production-release/v1.0-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LRU Brick Cache with moka</name>
  <files>crates/seisly_io/Cargo.toml, crates/seisly_io/src/cache.rs</files>
  <action>
    - Add 'moka' to crates/seisly_io/Cargo.toml.
    - Implement BrickCache struct using moka::sync::Cache.
    - Integrate cache into SeismicReader in crates/seisly_io.
    - Ensure cache size is configurable and thread-safe.
  </action>
  <verify>
    <automated>cargo test -p seisly_io --test cache_benchmarks</automated>
  </verify>
  <done>Seismic brick access uses an LRU cache, reducing disk I/O for repeated requests.</done>
</task>

<task type="auto">
  <name>Task 2: Sentry & Panic Dialogs</name>
  <files>crates/seisly_app/Cargo.toml, crates/seisly_app/src/main.rs</files>
  <action>
    - Add 'sentry' and 'rfd' to crates/seisly_app/Cargo.toml.
    - Initialize Sentry in main.rs.
    - Implement custom panic hook that shows an rfd::MessageDialog with the panic info before exiting.
  </action>
  <verify>
    <automated>cargo run -p seisly_app -- --test-panic</automated>
  </verify>
  <done>Panics are caught, reported to Sentry, and displayed to the user via a native dialog.</done>
</task>

<task type="auto">
  <name>Task 3: Surface Normal Computation</name>
  <files>crates/seisly_render/src/surface.rs</files>
  <action>
    - Refactor mesh generation in surface.rs to compute per-vertex normals.
    - Use cross product of neighboring face edges for normal calculation.
    - Update vertex buffer layout to include normal data (layout: pos, normal, color).
  </action>
  <verify>
    <automated>cargo test -p seisly_render --lib surface::tests::test_normals</automated>
  </verify>
  <done>3D surfaces have correct normals, enabling realistic lighting in the 3D viewport.</done>
</task>

</tasks>

<verification>
Manual verification of the panic dialog by triggering a test panic. Benchmarking brick access before and after cache.
</verification>

<success_criteria>
- Seismic bricks are cached and retrieved quickly.
- App crashes are handled gracefully with a user dialog.
- 3D surfaces look much better with proper lighting.
</success_criteria>

<output>
After completion, create .planning/phases/v1.0-production-release/v1.0-02-SUMMARY.md
</output>
