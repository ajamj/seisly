---
phase: v1.0-production-release
plan: 03
type: execute
wave: 2
depends_on: ["v1.0-01"]
files_modified: [
  "crates/seisly_py_worker/Cargo.toml",
  "crates/seisly_py_worker/src/main.rs",
  "crates/seisly_plugin/src/ipc.rs",
  "crates/seisly_plugin/src/lib.rs"
]
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "A crashing Python plugin only terminates the worker process, not the Seisly host"
    - "Host app can launch, communicate with, and restart the Python worker process"
  artifacts:
    - path: "crates/seisly_py_worker/src/main.rs"
      provides: "Process-isolated Python interpreter host"
    - path: "crates/seisly_plugin/src/ipc.rs"
      provides: "IPC bridge for plugin execution"
  key_links:
    - from: "seisly_plugin::PluginManager"
      to: "seisly_py_worker"
      via: "std::process::Command and JSON-RPC over stdin/stdout"
---

<objective>
Implement process isolation for Python plugins to ensure main application stability.

Purpose: Plugin security and fault tolerance.
Output: Separate worker process for Python execution with a stable IPC bridge.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.0-production-release/v1.0-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Isolated Python Worker Crate</name>
  <files>crates/seisly_py_worker/Cargo.toml, crates/seisly_py_worker/src/main.rs</files>
  <action>
    - Create a new binary crate 'seisly_py_worker'.
    - Move PyO3 initialization and NumPy bridging from seisly_plugin to this crate.
    - Implement a simple JSON-RPC loop over stdin/stdout for receiving plugin execution requests.
  </action>
  <verify>
    <automated>cargo build -p seisly_py_worker</automated>
  </verify>
  <done>Worker crate exists and can host a Python interpreter independently.</done>
</task>

<task type="auto">
  <name>Task 2: IPC Bridge Implementation</name>
  <files>crates/seisly_plugin/src/ipc.rs, crates/seisly_plugin/src/lib.rs</files>
  <action>
    - Implement IpcBridge in crates/seisly_plugin using std::process::Command to launch the worker.
    - Use channels (serde_json) for communication.
    - Refactor PythonPlugin to use this bridge instead of direct PyO3 calls.
    - Add worker crash detection and automatic restart logic.
  </action>
  <verify>
    <automated>cargo test -p seisly_plugin --test plugin_isolation_test</automated>
  </verify>
  <done>Python plugins run in the worker process; host app survives plugin crashes.</done>
</task>

</tasks>

<verification>
Run a 'malicious' plugin that triggers a segfault in Python and verify the Seisly UI remains responsive.
</verification>

<success_criteria>
- Python interpreter is no longer in the main process.
- Plugin crashes are isolated.
- Communication latency between host and worker is minimal.
</success_criteria>

<output>
After completion, create .planning/phases/v1.0-production-release/v1.0-03-SUMMARY.md
</output>
