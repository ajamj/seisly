---
phase: v1.0-production-release
plan: 05
type: execute
wave: 4
depends_on: ["v1.0-04"]
files_modified: [
  "Cargo.toml",
  ".github/workflows/release.yml",
  "CHANGELOG.md"
]
autonomous: true
requirements: [R8.3]
must_haves:
  truths:
    - "Running 'cargo dist' locally generates valid installers (MSI, PKG) and build artifacts"
    - "Pushing a tag (e.g., v1.0.0) triggers a GitHub Action that builds and uploads release artifacts"
    - "Release notes are automatically included in the GitHub Release"
  artifacts:
    - path: "dist.toml"
      provides: "cargo-dist configuration"
    - path: ".github/workflows/release.yml"
      provides: "CI/CD release pipeline"
  key_links:
    - from: "Git Tag"
      to: "GitHub Release"
      via: "GitHub Actions"
---

<objective>
Configure professional multi-platform distribution using cargo-dist and GitHub Actions.

Purpose: Automated distribution and installer generation.
Output: Automated release pipeline with installers for Windows and macOS.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.0-production-release/v1.0-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: cargo-dist Configuration</name>
  <files>Cargo.toml, dist.toml</files>
  <action>
    - Install cargo-dist locally (if not available).
    - Run 'cargo dist init' to configure release metadata in Cargo.toml.
    - Setup Windows MSI (using WiX) and macOS PKG targets.
    - Configure artifacts (main app binary, python worker binary, docs).
  </action>
  <verify>
    <automated>cargo dist plan</automated>
  </verify>
  <done>cargo-dist is configured to generate installers and archives for production targets.</done>
</task>

<task type="auto">
  <name>Task 2: Release CI/CD Pipeline</name>
  <files>.github/workflows/release.yml</files>
  <action>
    - Generate release.yml using 'cargo dist generate-ci'.
    - Update the workflow to include codesigning (placeholders for secrets).
    - Add steps to upload mdBook output (from v1.0-01) as part of the release artifacts.
    - Test the workflow by pushing a test tag to a dev branch (if possible).
  </action>
  <verify>
    <automated>cargo dist generate-ci</automated>
  </verify>
  <done>GitHub Actions are configured to automatically build and publish Seisly releases.</done>
</task>

<task type="auto">
  <name>Task 3: Final QA & Release Metadata</name>
  <files>CHANGELOG.md, crates/seisly_app/src/main.rs</files>
  <action>
    - Finalize CHANGELOG.md for the v1.0.0 release.
    - Verify app version (v1.0.0) in all Cargo.toml files.
    - Run full E2E smoke tests locally (import SEG-Y -> pick faults -> run python plugin).
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <done>Metadata is correct, and the application passes final quality checks for production.</done>
</task>

</tasks>

<verification>
Manual verification of the generated installer (MSI) on a clean Windows machine.
</verification>

<success_criteria>
- Reliable multi-platform installers.
- Automated release process.
- Consistent versioning (v1.0.0).
</success_criteria>

<output>
After completion, create .planning/phases/v1.0-production-release/v1.0-05-SUMMARY.md
</output>
