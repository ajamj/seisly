---
phase: v1.0-production-release
plan: 04
type: execute
wave: 3
depends_on: ["v1.0-02", "v1.0-03"]
files_modified: [
  "crates/seisly_app/Cargo.toml",
  "crates/seisly_app/src/ui/layout.rs",
  "crates/seisly_app/src/ui/mod.rs",
  "crates/seisly_app/src/ui/style.rs"
]
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "Users can rearrange and dock/undock UI panels via egui_dock"
    - "UI has a consistent color palette, spacing, and icon set across all panels"
    - "Common actions can be triggered via global keyboard shortcuts"
  artifacts:
    - path: "crates/seisly_app/src/ui/layout.rs"
      provides: "Dockable tab tree and panel rendering logic"
    - path: "crates/seisly_app/src/ui/style.rs"
      provides: "Global Seisly visual theme"
  key_links:
    - from: "seisly_app::App"
      to: "egui_dock::DockArea"
      via: "ui() method"
---

<objective>
Refine the UI/UX with dockable panels, professional styling, and keyboard shortcuts.

Purpose: Professional user experience and productivity.
Output: Flexible dockable UI with a polished, consistent visual identity.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.0-production-release/v1.0-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: egui_dock Integration</name>
  <files>crates/seisly_app/Cargo.toml, crates/seisly_app/src/ui/layout.rs</files>
  <action>
    - Add 'egui_dock' to crates/seisly_app/Cargo.toml.
    - Implement a DockablePanel enum representing existing panels (SeismicView, ProjectExplorer, Properties, PluginManager).
    - Implement the TabViewer trait for our App state.
    - Setup egui_dock::Tree in the App struct to manage panel layouts.
    - Enable layout persistence (save/load dock tree to/from disk).
  </action>
  <verify>
    <automated>cargo check -p seisly_app</automated>
  </verify>
  <done>UI panels are now tabs within a flexible, dockable layout.</done>
</task>

<task type="auto">
  <name>Task 2: Professional Styling & Widget Audit</name>
  <files>crates/seisly_app/src/ui/style.rs, crates/seisly_app/src/ui/mod.rs</files>
  <action>
    - Create a style.rs module to define a consistent theme (colors, spacing, rounding).
    - Apply this theme to the egui::Context in App::setup.
    - Audit all existing widgets (buttons, sliders, inputs) to ensure they use themed variants.
    - Implement a consistent header/footer and toolbar design across all docked panels.
  </action>
  <verify>
    <automated>cargo test -p seisly_app --test ui_smoke_test</automated>
  </verify>
  <done>UI looks polished, professional, and visually consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Shortcut Manager Implementation</name>
  <files>crates/seisly_app/src/app.rs, crates/seisly_app/src/ui/shortcuts.rs</files>
  <action>
    - Create a shortcuts.rs module to define global keyboard shortcuts (e.g., Ctrl+O for open, Ctrl+S for save, F5 for refresh).
    - Implement an input handling loop in App::update to catch and dispatch these shortcuts.
    - Add shortcut hints to tooltips and menu items for user discovery.
  </action>
  <verify>
    <automated>cargo test -p seisly_app --lib ui::shortcuts::tests</automated>
  </verify>
  <done>Keyboard shortcuts are functional and discoverable, improving user efficiency.</done>
</task>

</tasks>

<verification>
Manual verification of the UI layout (dragging tabs) and keyboard shortcuts.
</verification>

<success_criteria>
- Flexible panel management.
- Polished, professional-grade visual theme.
- High-efficiency navigation via shortcuts.
</success_criteria>

<output>
After completion, create .planning/phases/v1.0-production-release/v1.0-04-SUMMARY.md
</output>
