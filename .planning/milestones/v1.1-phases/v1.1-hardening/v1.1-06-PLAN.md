---
phase: v1.1-hardening
plan: 06
type: execute
wave: 5
depends_on: []
files_modified: [crates/seisly_core/src/domain/surface.rs, crates/seisly_render/src/mesh.rs, crates/seisly_compute/src/triangulation.rs]
autonomous: true
requirements: [HARD-05]

must_haves:
  truths:
    - "Shaded horizons appear smooth and continuous in the 3D view"
    - "Normal computation is correctly weighted for varying triangle sizes"
  artifacts:
    - path: "crates/seisly_core/src/domain/surface.rs"
      provides: "Weighted normal averaging implementation"
  key_links:
    - from: "seisly_render"
      to: "seisly_core::Mesh::compute_normals"
      via: "rendering setup"
---

<objective>
Improve visual quality of shaded surfaces through high-quality normal averaging.

Purpose: Faceted surfaces are common when using simple normal averaging. This plan implements weighted normal averaging to ensure smooth horizons.
Output: Weighted normal computation logic and consistent integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/seisly_core/src/domain/surface.rs
@crates/seisly_render/src/mesh.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weighted Normal Averaging</name>
  <files>crates/seisly_core/src/domain/surface.rs</files>
  <action>
    - Upgrade `Mesh::compute_normals` to use weighted face normals (weight by triangle area or angle at vertex). Area-weighted normals prevent small triangles from disproportionately affecting the surface look.
    - Reference v1.0 reviews: "Ensure 'Normal Averaging' is used for smooth-shaded horizons to avoid faceted looks."
  </action>
  <verify>
    <automated>cargo test -p seisly_core --lib domain::surface</automated>
  </verify>
  <done>Smooth normals are now computed using area-weighted face contributions.</done>
</task>

<task type="auto">
  <name>Task 2: Consistent Normal Integration</name>
  <files>crates/seisly_render/src/mesh.rs, crates/seisly_compute/src/triangulation.rs, crates/seisly_ml/src/tracker.rs, crates/seisly_compute/src/clipping.rs</files>
  <action>
    - Audit all locations where `Mesh` is instantiated (e.g. tracker, triangulation, clipping).
    - Ensure `compute_normals()` is called before passing the mesh to the renderer.
    - Update `seisly_render/src/mesh.rs` to log a warning if a mesh without normals is being rendered in shaded mode.
  </action>
  <verify>
    <automated>cargo build -p seisly_render</automated>
  </verify>
  <done>All system-generated meshes now use smooth shading by default.</done>
</task>

</tasks>

<verification>
Load a horizon from an XYZ file or autotrack one and confirm it appears smooth in the 3D scene under directional lighting.
</verification>

<success_criteria>
- No "faceted" artifacts on horizon surfaces.
- Correct normals for arbitrary meshes (including clipped or triangulated ones).
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1-hardening/v1.1-06-SUMMARY.md`
</output>
