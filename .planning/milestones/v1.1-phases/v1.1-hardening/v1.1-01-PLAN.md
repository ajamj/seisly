---
phase: v1.1-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [crates/seisly_core/src/ipc/shm.rs, crates/seisly_py_worker/src/shm.rs, crates/seisly_plugin/src/ipc.rs]
autonomous: true
requirements: [HARD-01]

must_haves:
  truths:
    - "Host can allocate a shared memory segment and write data"
    - "Worker can attach to the segment and read the data"
    - "Data integrity is maintained across process boundary"
  artifacts:
    - path: "crates/seisly_core/src/ipc/shm.rs"
      provides: "Rust SHM implementation"
    - path: "crates/seisly_py_worker/src/shm.rs"
      provides: "Python-side SHM bridge"
  key_links:
    - from: "seisly_plugin"
      to: "seisly_py_worker"
      via: "shared memory segment ID"
---

<objective>
Establish a high-performance Shared Memory (SHM) transport layer for seismic data.

Purpose: JSON-RPC over stdio is the primary bottleneck for large seismic arrays. SHM allows zero-copy or near-zero-copy transfer of multi-gigabyte arrays.
Output: Rust/Python SHM bridge using the `shared_memory` crate.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/seisly_plugin/src/ipc.rs
@crates/seisly_py_worker/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Rust SHM Implementation</name>
  <files>crates/seisly_core/src/ipc/shm.rs, crates/seisly_core/src/lib.rs</files>
  <action>
    - Add `shared_memory` and `raw-window-handle` (if needed for cross-process sync) to `seisly_core/Cargo.toml`.
    - Implement `ShmSegment` struct that wraps `shared_memory::Shmem`.
    - Provide `create(size)` and `open(id)` methods.
    - Implement `write_data(&[u8])` and `read_data(&mut [u8])`.
    - Ensure proper cleanup (detach/unlink) on drop.
  </action>
  <verify>
    <automated>cargo test -p seisly_core --lib ipc::shm</automated>
  </verify>
  <done>Rust-side SHM segment management is functional.</done>
</task>

<task type="auto">
  <name>Task 2: Python Worker SHM Support</name>
  <files>crates/seisly_py_worker/src/shm.rs, crates/seisly_py_worker/src/main.rs</files>
  <action>
    - Add SHM support to `seisly_py_worker`.
    - Implement a method to map an existing SHM segment by ID into a NumPy array using `PyArrayDyn::from_ptr`.
    - Update `handle_request` in `main.rs` to accept a new `load_shm` command that takes a segment ID and shape.
    - Reference: v1.0 reviews suggested Shared Memory for seismic arrays.
  </action>
  <verify>
    <automated>cargo build -p seisly_py_worker</automated>
  </verify>
  <done>Python worker can attach to SHM segments and interpret them as NumPy arrays.</done>
</task>

<task type="auto">
  <name>Task 3: Integration Test for Cross-Process SHM</name>
  <files>crates/seisly_plugin/src/ipc.rs, crates/seisly_plugin/tests/shm_test.rs</files>
  <action>
    - Add a `transfer_shm` method to `IpcBridge` in `seisly_plugin/src/ipc.rs`.
    - Write an integration test that:
      1. Creates a SHM segment in the host.
      2. Writes 1MB of random data.
      3. Sends the SHM ID to the worker via JSON-RPC.
      4. Worker reads the data and returns a checksum.
      5. Host verifies the checksum.
  </action>
  <verify>
    <automated>cargo test --test shm_test</automated>
  </verify>
  <done>Verified high-performance data transfer between host and worker via SHM.</done>
</task>

</tasks>

<verification>
Verify that SHM transfer is significantly faster than JSON-RPC for 10MB+ payloads.
</verification>

<success_criteria>
- `ShmSegment` works on Windows and Linux.
- Python worker can access host-allocated memory without copying.
- Zero-copy path established for seismic arrays.
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1-hardening/v1.1-01-SUMMARY.md`
</output>
