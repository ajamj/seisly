---
phase: v1.1-hardening
plan: 03
type: execute
wave: 2
depends_on: []
files_modified: [crates/seisly_plugin/src/ipc.rs, crates/seisly_py_worker/src/main.rs]
autonomous: true
requirements: [HARD-02]

must_haves:
  truths:
    - "Host can detect if the worker process hangs or crashes"
    - "Worker is automatically restarted if heartbeats stop"
    - "Worker's memory usage is capped at an OS-level"
  artifacts:
    - path: "crates/seisly_plugin/src/ipc.rs"
      provides: "Watchdog and resource quota logic"
  key_links:
    - from: "seisly_plugin"
      to: "seisly_py_worker"
      via: "background heartbeat thread"
---

<objective>
Harden the Python worker against hangs and resource exhaustion.

Purpose: Python plugins may run into infinite loops or consume too much RAM. The host must remain stable and be able to recover.
Output: Background watchdog and OS-level process quotas.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/seisly_plugin/src/ipc.rs
@crates/seisly_py_worker/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Background Heartbeat & Watchdog</name>
  <files>crates/seisly_plugin/src/ipc.rs, crates/seisly_py_worker/src/main.rs</files>
  <action>
    - Add a background thread in `IpcBridge` that sends a `ping` request to the worker every 5 seconds.
    - If a ping fails (timeout or error), log a warning and increment a failure counter.
    - If failures exceed a threshold (e.g., 3), kill the worker and mark the bridge as needing a restart.
    - Implement the `ping` response in `seisly_py_worker/src/main.rs`.
    - Per v1.0 reviews: "Add heartbeats and watchdog logic."
  </action>
  <verify>
    <automated>cargo test -p seisly_plugin --lib ipc -- --nocapture</automated>
  </verify>
  <done>Host monitors worker health and can detect hangs.</done>
</task>

<task type="auto">
  <name>Task 2: OS-Level Resource Quotas</name>
  <files>crates/seisly_plugin/src/ipc.rs</files>
  <action>
    - When spawning the worker process, set resource limits if the platform supports it.
    - Linux: Use `prlimit` to set RSS memory caps.
    - Windows: Use `Job Objects` to set memory and CPU limits.
    - Use the `sysinfo` crate (if available or add it) to monitor the worker's RAM usage and kill if it exceeds the user-configured limit.
    - Per v1.0 reviews: "Add resource quotas (memory/CPU)."
  </action>
  <verify>
    <automated>cargo build -p seisly_plugin</automated>
  </verify>
  <done>Python worker is constrained to a predefined resource envelope.</done>
</task>

</tasks>

<verification>
Verify that killing the worker manually results in the host detecting the crash and being ready to restart on the next plugin call.
</verification>

<success_criteria>
- Worker hangs no longer freeze the main UI thread.
- Worker memory leaks are capped by the OS or the watchdog.
- Automatic recovery from worker process failure.
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1-hardening/v1.1-03-SUMMARY.md`
</output>
