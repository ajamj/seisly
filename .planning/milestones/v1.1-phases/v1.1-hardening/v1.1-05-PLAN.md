---
phase: v1.1-hardening
plan: 05
type: execute
wave: 4
depends_on: []
files_modified: [crates/seisly_core/src/commands/mod.rs, crates/seisly_app/src/app.rs, crates/seisly_tracking/src/lib.rs]
autonomous: true
requirements: [HARD-04]

must_haves:
  truths:
    - "User can undo a horizon pick"
    - "User can redo a previously undone pick"
    - "Pick modifications are atomic and crash-safe in SQLite"
  artifacts:
    - path: "crates/seisly_core/src/commands/undo_redo.rs"
      provides: "Command stack implementation"
  key_links:
    - from: "seisly_app"
      to: "seisly_core::commands"
      via: "centralized command execution"
---

<objective>
Implement a centralized Command pattern for Undo/Redo and ensure all interpretations are transaction-safe.

Purpose: Production-grade interpretation requires reliable data modification. Per v1.0 reviews: "Lack of a centralized Command/Undo-Redo pattern."
Output: Command stack and transactionally wrapped interpretation operations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/seisly_tracking/src/lib.rs
@crates/seisly_app/src/app.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Undo/Redo Stack & Command Pattern</name>
  <files>crates/seisly_core/src/commands/undo_redo.rs, crates/seisly_core/src/lib.rs</files>
  <action>
    - Create a `Command` trait with `execute` and `undo` methods.
    - Implement an `UndoRedoStack` that manages a history of commands.
    - Add basic commands for testing (e.g. "AddPoint", "DeletePoint").
    - Per v1.0 reviews: "Add a centralized Command/Undo-Redo pattern."
  </action>
  <verify>
    <automated>cargo test -p seisly_core --lib commands</automated>
  </verify>
  <done>Foundational Undo/Redo infrastructure is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Transactional Picking Integration</name>
  <files>crates/seisly_tracking/src/lib.rs, crates/seisly_app/src/app.rs</files>
  <action>
    - Update the horizon picking logic to use `Command` objects.
    - Ensure that any pick modification to the SQLite database is wrapped in a transaction.
    - Integrate the command stack into the main `App` state in `seisly_app`.
    - Map Ctrl+Z and Ctrl+Y shortcuts to undo/redo actions.
  </action>
  <verify>
    <automated>cargo test -p seisly_tracking</automated>
  </verify>
  <done>User actions are now reversible and data persistence is crash-safe.</done>
</task>

</tasks>

<verification>
Run the app, perform several picking operations, use Ctrl+Z to undo them, and verify that the database state matches the expected (undone) state.
</verification>

<success_criteria>
- Reliable Undo/Redo functionality for primary interpretation tasks.
- No partial pick updates in the database on crash (all-or-nothing transactions).
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1-hardening/v1.1-05-SUMMARY.md`
</output>
