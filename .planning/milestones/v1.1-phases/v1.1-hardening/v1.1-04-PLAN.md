---
phase: v1.1-hardening
plan: 04
type: execute
wave: 3
depends_on: []
files_modified: [crates/seisly_io/src/segy/mmap.rs, crates/seisly_core/src/io/safe_mmap.rs]
autonomous: true
requirements: [HARD-03]

must_haves:
  truths:
    - "Reading from a memory-mapped file that fails (SIGBUS) does not crash the process"
    - "Errors during memory mapping are captured and returned as standard Result::Err"
  artifacts:
    - path: "crates/seisly_core/src/io/safe_mmap.rs"
      provides: "SIGBUS-protected memory access"
  key_links:
    - from: "seisly_io"
      to: "seisly_core::io::safe_mmap"
      via: "wrapped memory access"
---

<objective>
Protect the application from SIGBUS/exception crashes caused by memory-mapped file failures.

Purpose: `memmap2` is fast but unsafe when the file disappears or the disk fails. This plan adds a safety wrapper.
Output: Safe memory mapping bridge.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/seisly_io/src/segy/mmap.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: SIGBUS/Exception Protection Wrapper</name>
  <files>crates/seisly_core/src/io/safe_mmap.rs, crates/seisly_core/src/lib.rs</files>
  <action>
    - Implement a `SafeMmap` wrapper that provides guarded access to a memory-mapped region.
    - Windows: Use structured exception handling (`__try/__except` via `windows-rs` or `std::panic::catch_unwind` if it can catch SEH). Actually, use the `memmap2`'s `Mmap::make_safe` if available, or use the `signal-hook` crate for Linux.
    - Research: `memmap2` on crates.io has a `safe` module for some platforms. Use it if possible.
    - Goal: Turn a SIGBUS/Access Violation into a `std::io::Error`.
    - Per v1.0 reviews: "Implement a 'Safe Mapper' wrapper around memmap2."
  </action>
  <verify>
    <automated>cargo build -p seisly_core</automated>
  </verify>
  <done>Core safety wrapper for memory mapping is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Safe Mapping in Seisly IO</name>
  <files>crates/seisly_io/src/segy/mmap.rs</files>
  <action>
    - Replace `memmap2::Mmap` with the new `SafeMmap` in `MmappedSegy`.
    - Update the trace reading logic to handle potential I/O errors from the safe wrapper.
    - Ensure errors are propagated up to the caller (usually the renderer or data cache).
  </action>
  <verify>
    <automated>cargo test -p seisly_io --lib segy::mmap</automated>
  </verify>
  <done>Seismic I/O is hardened against file/network failures.</done>
</task>

</tasks>

<verification>
Simulate a file failure (e.g. by truncating the file while it is mapped) and verify the application returns an error instead of crashing.
</verification>

<success_criteria>
- SIGBUS/Access Violation crashes are eliminated during seismic data access.
- Application handles network-mapped file failures gracefully.
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1-hardening/v1.1-04-SUMMARY.md`
</output>
