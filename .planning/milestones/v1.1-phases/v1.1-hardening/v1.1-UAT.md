---
status: testing
phase: v1.1-hardening
source: v1.1-01-SUMMARY.md, v1.1-02-SUMMARY.md, v1.1-03-SUMMARY.md, v1.1-04-SUMMARY.md, v1.1-05-SUMMARY.md, v1.1-06-SUMMARY.md
started: 2026-04-01T12:00:00.000Z
updated: 2026-04-01T12:00:00.000Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Cold Start Smoke Test
expected: |
  Kill any running server/service. Clear ephemeral state (temp DBs, caches, lock files). Start the application from scratch. Server boots without errors, any seed/migration completes, and a primary query (health check, homepage load, or basic API call) returns live data.
awaiting: user response

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running Seisly instances. Clear temp files and caches. Start seisly-app from scratch. Application boots without errors, loads project successfully, and displays the main viewport with seismic data.
result: pending

### 2. SHM Large Data Transfer (HARD-01)
expected: Load a large seismic volume (>10MB). Transfer data to Python worker for processing. Transfer completes in <5 seconds (vs 10-100x slower with JSON-RPC). No memory errors or crashes.
result: pending

### 3. API Version Handshake (HARD-01/02)
expected: Start Seisly app with Python worker. Check logs for handshake message showing protocol version "1.1" negotiation. Handshake completes successfully before any IPC operations.
result: pending

### 4. Worker Heartbeat Monitoring (HARD-02)
expected: Run a Python plugin that takes >10 seconds. Observe heartbeat pings in logs every 5 seconds. Worker responds to each ping. No "worker hung" warnings for healthy workers.
result: pending

### 5. Memory Limit Warning (HARD-02)
expected: Run a memory-intensive Python plugin that approaches 512MB. Observe memory warning in logs when limit is exceeded. Application remains stable and responsive.
result: pending

### 6. SIGBUS Protection (HARD-03)
expected: Open a SEG-Y file from network location. Simulate network disconnection during read. Application returns error message instead of crashing. No SIGBUS/Access Violation exceptions.
result: pending

### 7. Undo Horizon Pick (HARD-04)
expected: Pick a horizon point manually. Press Ctrl+Z. Pick disappears from viewport and database. Press Ctrl+Y. Pick reappears. Undo/redo stack shows operation history.
result: pending

### 8. Undo Auto-Track (HARD-04)
expected: Run auto-fault tracking. Multiple picks are added. Press Ctrl+Z. All auto-tracked picks are removed in single undo operation. Press Ctrl+Y. All picks are restored.
result: pending

### 9. SQLite Transaction Safety (HARD-04)
expected: Make multiple picks rapidly. Force close application during pick operation. Reopen application. Database is consistent - either all picks saved or none (no partial state).
result: pending

### 10. Smooth Shaded Horizons (HARD-05)
expected: Load or create a horizon surface. View in 3D viewport with directional lighting. Surface appears smooth and continuous, not faceted. Normal interpolation works correctly across triangle boundaries.
result: pending

### 11. Clipped Surface Normals (HARD-05)
expected: Clip a horizon surface with a plane. Clipped surface maintains smooth shading. No visual artifacts at clip boundary. Normals computed correctly for split mesh.
result: pending

### 12. Log Strip Visualization (HARD-05)
expected: Display well log strips in 3D viewport. Log strips have proper lighting and shading. Normals computed for log strip meshes.
result: pending

## Summary

total: 12
passed: 0
issues: 0
pending: 12
skipped: 0
blocked: 0

## Gaps

<!-- YAML format for plan-phase --gaps consumption -->
<!-- Will be populated if issues found during testing -->
