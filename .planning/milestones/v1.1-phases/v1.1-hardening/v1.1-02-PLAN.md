---
phase: v1.1-hardening
plan: 02
type: execute
wave: 2
depends_on: ["v1.1-01"]
files_modified: [crates/seisly_plugin/src/bridge.rs, crates/seisly_plugin/src/python_plugin.rs, crates/seisly_py_worker/src/main.rs]
autonomous: true
requirements: [HARD-01, HARD-02]

must_haves:
  truths:
    - "Seismic data slices are automatically transferred via SHM if they exceed a size threshold"
    - "Host and worker negotiate an API version on startup"
  artifacts:
    - path: "crates/seisly_plugin/src/bridge.rs"
      provides: "SHM-aware data bridging"
  key_links:
    - from: "seisly_plugin"
      to: "seisly_py_worker"
      via: "handshake JSON-RPC call"
---

<objective>
Migrate the data bridge to use high-performance SHM and implement a versioned handshake.

Purpose: Automate the use of SHM for large data transfers and ensure host-worker compatibility.
Output: Integrated SHM bridging and versioned IPC handshake.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.1-hardening/v1.1-01-PLAN.md
@crates/seisly_plugin/src/bridge.rs
@crates/seisly_plugin/src/python_plugin.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: SHM-Aware Data Bridging</name>
  <files>crates/seisly_plugin/src/bridge.rs, crates/seisly_plugin/src/ipc.rs</files>
  <action>
    - Update `seisly_plugin/src/bridge.rs` to detect when a data slice is large (e.g., > 1MB).
    - If large, allocate a `ShmSegment` (from Plan 01), write data, and send the segment ID to the worker.
    - If small, continue using JSON-RPC inline transfer for simplicity.
    - Ensure `IpcBridge` supports sending segment references.
  </action>
  <verify>
    <automated>cargo test -p seisly_plugin --lib bridge</automated>
  </verify>
  <done>Large data transfers automatically switch to the high-performance SHM path.</done>
</task>

<task type="auto">
  <name>Task 2: API Version Handshake</name>
  <files>crates/seisly_plugin/src/ipc.rs, crates/seisly_py_worker/src/main.rs</files>
  <action>
    - Add a `handshake` method to the worker's JSON-RPC protocol.
    - The `IpcBridge` must call `handshake` immediately after spawning the worker.
    - Parameters: `{ "protocol_version": "1.1", "shm_supported": true }`.
    - If versions are incompatible, the host should log a clear error and refuse to proceed.
    - Per v1.0 reviews: "Add schema negotiation for API versioning between host and plugin."
  </action>
  <verify>
    <automated>cargo test -p seisly_plugin --lib ipc</automated>
  </verify>
  <done>Host and worker successfully negotiate API compatibility on startup.</done>
</task>

</tasks>

<verification>
Start host and worker, verify handshake in logs, and confirm large slice transfer via SHM.
</verification>

<success_criteria>
- Large seismic arrays are no longer JSON-serialized.
- Version mismatch results in a graceful startup failure with an informative message.
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1-hardening/v1.1-02-SUMMARY.md`
</output>
