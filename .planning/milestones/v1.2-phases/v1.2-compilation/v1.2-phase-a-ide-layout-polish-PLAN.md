---
phase: v1.2-phase-a-ide-layout-polish
epic: IDE Layout Refactor
wave: 1
autonomous: true
objective: Finalize the modern IDE layout implementation with theme persistence, settings integration, and activity bar completion.
---

# Plan: IDE Layout Polish & Persistence

## Context
Milestone 1 is complete. The IDE layout refactor is partially implemented in `app.rs` and `style.rs`. This plan completes the missing features according to the `2026-04-07-ide-layout-design.md` spec.

## Work Units

### WU-006: Theme Persistence
- **Description:** Ensure the user's theme selection (Dark/Light) persists across application restarts.
- **Tasks:**
  - Update `SeislyApp::save` to store the current theme name in `eframe` storage.
  - Update `SeislyApp::new` to load the theme name from storage and apply it via `ThemeManager`.
- **Verification:** `cargo check` passes.

### WU-007: Settings Panel Refactor
- **Description:** Provide a user-friendly theme switcher in the Settings panel.
- **Tasks:**
  - Update `crates/seisly_app/src/widgets/settings_panel.rs` to include a theme selector (Dark/Light).
  - Ensure the selection updates the app's `ThemeManager`.
- **Verification:** `cargo check` passes.

### WU-008: Activity Bar Completion
- **Description:** Add missing Diagnostics toggle and correct icons to the Activity Bar.
- **Tasks:**
  - Create `crates/seisly_app/assets/icons/terminal.svg` (for Logs).
  - Add `SidebarTab::Diagnostics` variant.
  - Update `render_activity_bar` to include the Diagnostics toggle.
  - Update `activity_button` to handle the new tab.
- **Verification:** `cargo check` passes.

### WU-009: Design Alignment & Checklist
- **Description:** Final pass to ensure all success criteria from the design doc are met.
- **Tasks:**
  - Review all components for semantic token usage.
  - Update `docs/superpowers/specs/2026-04-07-ide-layout-design.md` success criteria.
- **Verification:** All boxes checked.

## Success Criteria
- [ ] Theme selection persists after restart.
- [ ] Settings panel has a functional theme switcher.
- [ ] Activity Bar has functional toggles for Explorer, Interpretation, and Logs.
- [ ] UI uses Lucide monochromatic icons consistently.
