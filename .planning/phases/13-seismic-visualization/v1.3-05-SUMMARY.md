---
phase: v1.3
plan: 05
type: gap-closure
completed: 2026-04-09
status: completed
files_modified:
  - crates/seisly_app/src/app.rs
  - crates/seisly_app/src/widgets/viewport.rs
---

# Summary: v1.3-05 - UI Polish: Status Bar, Slider Values, Label Consistency

## What Was Accomplished

### WU-1314: Remove fake status bar coordinates
- **File:** `crates/seisly_app/src/app.rs` (lines ~785-810)
- Removed hardcoded `"X: 250.5  Y: 312.8  Z: 1523m "` string
- Replaced with dynamic status text:
  - When volume loaded: shows `"Volume: {name}"` (from visible `SeismicVolumeEntry`) or `"Seismic data loaded"` as fallback
  - When no volume: shows `"No seismic data loaded"`
  - Right-aligned: shows survey extents `IL:{min}-{max} XL:{min}-{max} S:{samples}` when volume is loaded
- Uses `.monospace()` font for all status bar text

### WU-1315: Show Gain/Clip slider values
- **File:** `crates/seisly_app/src/widgets/viewport.rs` (lines ~104-113)
- Changed both sliders from `.show_value(false)` to `.show_value(true)`
- Added hover tooltips via `.on_hover_text()`:
  - Gain: "Amplitude gain multiplier (default: 1.0)"
  - Clip: "Clip level for amplitude normalization (default: 1.0)"

### WU-1316: Consistent "Time Slice" label
- **File:** `crates/seisly_app/src/widgets/viewport.rs`
- Changed radio button label from `"Time"` to `"Time Slice"` (line 66)
- Changed time slider text from `"Time"` to `"Time Slice"` (line 97)
- Verified no orphaned `"Time"` labels remain for slice type

## Verification

- `cargo check -p seisly_app` -- passes
- `cargo clippy -p seisly_app -- -W clippy::all` -- passes (all warnings pre-existing)
- `grep -n "250.5\|312.8\|1523m" app.rs` -- zero matches
- `grep -n '"Time"' viewport.rs` -- zero matches
- `grep -n '"Time Slice"' viewport.rs` -- 2 matches (radio button + slider text)
